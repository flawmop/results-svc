apiVersion: v1
kind: Service
metadata:
  name: results-svc
  labels:
    app: results-svc
spec:
  type: ClusterIP
  selector:
    app: results-svc
  ports:
  - protocol: TCP
    port: 80
    targetPort: 9004
